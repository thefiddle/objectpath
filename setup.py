from setuptools import setup, find_packages

setup(
    name='objectpath',
    version="0.1",
    description="objectpath language processor",
    author='Guillaume Lafargue',
    author_email='guillaume@newzulu.com',
    packages=["objectpath", "objectpath.core", "objectpath.utils"]
)
